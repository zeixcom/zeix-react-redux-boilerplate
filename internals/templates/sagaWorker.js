import { call, put } from 'redux-saga/effects';
import axios from 'axios';

export default function* work.NAME. (action) {
  try {

    // call api via yield call

    // put api response to our store yield put

  } catch (error) {
    console.error('Request Failed (.NAME.):');
    console.error(error);
  }
}